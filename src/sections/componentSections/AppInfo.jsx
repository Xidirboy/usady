import React from "react";
import styled from "styled-components";
const AppInfoStyle = styled.div``;
const AppInfo = () => {
  return <AppInfoStyle>AppInfo</AppInfoStyle>;
};

export default AppInfo;
